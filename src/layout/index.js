import styled from "styled-components";
import { motion } from "framer-motion";
import { Navbar } from "react-bootstrap";
// ! Header

export const AnimatedStyledNavbar = styled(motion.custom(Navbar))`
  background-color: #6930c3 !important;
`;
